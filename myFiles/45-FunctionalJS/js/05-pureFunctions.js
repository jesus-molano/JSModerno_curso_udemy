// Funciones puras - Pure Function
// Funciones que retornan un dato pero no modifican el valor de las variables
const numero1 = 20;
const duplicar = (num) => num * 2;

const resultado = duplicar(numero1);

console.log(resultado);
console.log(numero1);
